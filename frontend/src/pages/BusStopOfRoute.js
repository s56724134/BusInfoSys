import React from 'react';
import { Container, Typography } from '@mui/material';

const BusStopOfRoute = () => {
  return (
    <Container>
      <Typography variant="h4" component="h1" gutterBottom>
        公車站點列表
      </Typography>
      <Typography variant="body1">
        這裡將會是公車站點列表頁面
      </Typography>
    </Container>
  );
};

export default BusStopOfRoute;