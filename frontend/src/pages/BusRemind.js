import React from 'react';
import { Container, Typography } from '@mui/material';

const BusRemind = () => {
  return (
    <Container>
      <Typography variant="h4" component="h1" gutterBottom>
        提醒設定
      </Typography>
      <Typography variant="body1">
        這裡將會是提醒設定頁面
      </Typography>
    </Container>
  );
};

export default BusRemind;